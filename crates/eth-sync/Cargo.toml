[package]
name = "eth-sync"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
common = { path = "../common" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
